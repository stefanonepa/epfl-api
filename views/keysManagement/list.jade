extends ../layout

block content
    a(href="/app/keysManagement/add") ADD
    if keys.length > 0
        ul
          each key in keys
            li= key
                a(href="/app/keysManagement/delete/#{key}") DEL